import { ChangeEvent, FC } from 'react';

interface PromptDisplayProps {
}

const PromptDisplay: FC<PromptDisplayProps> = ({
}) => {
  return (
    <div>
      <p>this is a thing where the cool stuff will go</p>
    </div>
  )
};

export default PromptDisplay
